package app.lovable.avishkar.services

import android.content.Context

object ImageManager {
    fun initialize(context: Context, supabaseUrl: String, supabaseKey: String) {
        // Basic initialization - can be expanded later
        println("ImageManager initialized with URL: $supabaseUrl")
    }
}
